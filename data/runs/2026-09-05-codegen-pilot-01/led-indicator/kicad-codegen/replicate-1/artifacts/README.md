# LED power indicator — KiCad code generation

Final entry files: `attempt-03/indicator.kicad_pro`, `attempt-03/indicator.kicad_sch`, and `attempt-03/indicator.kicad_pcb`. The editable source generator is `attempt-03/generate.py`; its dependency is the installed KiCad 10 Python pcbnew binding. Native files open independently of the generator. Exact standard symbol/footprint dependencies are bundled in `attempt-03/libraries` with project-local library tables.

The rectangular board is 30 × 20 mm with F.Cu and B.Cu. All connections use 0.30 mm F.Cu tracks; B.Cu is available and unused. Project and custom rules enforce 0.25 mm clearance and minimum track width. J1 is a 2.54 mm through-hole 1×2 header, with pin 1 (+5V) at (103,108.73) mm and pin 2 (GND) at (103,111.27) mm. The header centre lies 3 mm from the left edge at x=100. D1's centre is (127,108.73), 3 mm from the right edge at x=130. All footprint bounds are inside the board.

R1 is a generic 1 kohm 0805 resistor: pad 1 +5V, pad 2 LED_A. D1 is a generic red 0805 LED: pad 1 cathode/GND and pad 2 anode/LED_A, matching KiCad's standard LED symbol and LED_0805_2012Metric footprint. J1 pin 1 is square and explicitly labelled `1 +5V`; pin 2 is labelled `2 GND`. The LED cathode has the standard footprint mark and additional `K` silkscreen. Actual LED ordering must use this pad polarity and package; no manufacturer part number was requested or selected.

The schematic contains real wires and named nets. KiCad exported its netlist and SVG successfully. Final native ERC found zero errors/warnings; final PCB DRC with schematic parity found zero violations, zero unconnected pads, and zero parity issues. Reports are `../logs/attempt-03-erc.rpt` and `../logs/attempt-03-drc.rpt`; exact validation commands and console output are alongside them. SVG exports supplement the editable native files.

Preserved attempts: attempt-01 stopped at a Python enum mismatch after creating its schematic; attempt-02 created the board but reported missing library registration and differing local net name prefixes; attempt-03 corrects those with bundled libraries and matching names. Attempt-02's first PCB SVG export also failed because a file output option was passed a directory; attempt-03 uses a filename. Every original candidate and diagnostic is retained.

Method: direct native schematic text generation using standard embedded KiCad symbols, and pcbnew API board generation; shell only, no GUI. The Python binding emitted a wxApp initialization assertion message but completed final generation; this raw message is retained. No manufacturing exports, physical testing, simulation, or manufacturer-specific LED qualification was performed. No unresolved requested layout or connectivity requirements were found by the recorded checks. ERC/DRC use KiCad's default check enablement as listed in the reports; no violation exclusions were added.
