# LED indicator — tscircuit pilot

Final source: `attempt-02/index.circuit.tsx`. Compile with `tsci build index.circuit.tsx --pcb-png --schematic-png --svgs` in that directory using tsci 0.0.2462. `dist/index/` contains editable circuit JSON and schematic/PCB SVG and PNG previews. The package declaration and installed environment lock snapshot are retained.

J1 pin 1 is +5V (V5), pin 2 GND. R1 is 1 kohm / 0805. Generic red D1 is 0805, pad 1 anode and pad 2 cathode; K marks the cathode. Both footprints lie 4 mm from their requested board edges. The two-layer board is 30 × 20 mm. Three traces route all requested connections, at 0.30 mm width.

Attempt-01 retained an incorrect cathode text location and ineffective nested routing settings. Attempt-02 corrects the text and uses supported top-level 0.30 mm trace-to-pad/pad-to-pad clearance properties. Netlist, schematic placement, PCB placement, build, and shorts checks pass. Compiled circuit JSON has no error records; a missing schematic-sheet drawing-area warning remains. An independent full copper-clearance audit and physical testing are unmeasured. Generic components have no assigned manufacturer SKU.

This run was generated by the parent after subagent creation and follow-up hit the thread limit. It reused orchestration context and must not be described as a fresh-context run. All original candidates and logs are retained.
