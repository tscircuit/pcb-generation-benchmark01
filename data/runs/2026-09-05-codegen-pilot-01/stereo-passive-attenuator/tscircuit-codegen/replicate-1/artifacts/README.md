# Stereo AC-coupled attenuator

Final entry: `attempt-03/index.circuit.tsx`. Generated editable schematic and routed PCB representation: `attempt-03/dist/index/circuit.json`. The same directory contains `schematic.svg`, `pcb.svg`, and `pcb.png`. Rebuild with `tsci build index.circuit.tsx --svgs --pcb-png --disable-parts-engine` from the attempt directory using tscircuit 0.0.2462. The source is authoritative; SVG/PNG are previews.

J1/J2 are built-in male three-pin 2.54 mm through-hole headers. Pin 1 is the square pad, marked on silkscreen; pin labels identify the signals. C1/C2 are nonpolar generic 1 uF ceramic capacitors, 0805. R1–R4 are generic 10 kohm 0805 resistors. TP1/TP2/TP3 are exposed 1.5 mm circular copper pads for LEFT_OUT, RIGHT_OUT, GND. No manufacturer part number is assigned. Positive PCB Y appears toward the top in the exported preview; C1/R1/R2 are in this half. J1 and J2 centres are 3 mm from the respective edges.

Method: handwritten tscircuit TSX, explicit net traces, local autorouter, shell-only CLI. No KiCad or GUI creation. `tool-versions.json` records actual package versions. Each attempt preserves the installed environment lockfile as `environment.bun.lock` (a snapshot of the global installation, not a minimal project lockfile); its node_modules symlink points to that installed environment.

Three candidates are preserved. Attempt 01 compiled but net connectsTo declarations left all ports unconnected. Attempt 02 introduced explicit traces but had copper clearance errors. Attempt 03 rotated the headers, increased autorouter clearance and repaired schematic placement. All final requested CLI checks exit zero; copper shorts check says no shorts. An independent terminal graph audit confirms every declared net's terminals are connected by PCB trace objects, and verifies 50 x 30 mm, two layers and all trace widths 0.25 mm.

Unresolved: full 0.25 mm copper clearance is not independently verified. Although source requests 0.25 mm routingTolerances and 0.35 mm autorouter traceClearance, compiled board trace-to-pad and pad-to-pad rule fields remain at 0.1 mm; therefore the clean tool checks do not establish the requested clearance everywhere. Build warnings report no explicit schematic sheet and no testpoint courtyards. React reports missing list keys. No further repair was made because the maximum two repairs were used. Shorts checks for attempts 01/02 initially used an incorrect output path and failed; those raw diagnostics are preserved. The final shorts command uses the correct path. No fabrication exports or physical testing were requested or performed.
