# I2C connector and pull-up hub

Final editable entry: `attempt-03/index.circuit.tsx`. Compiled editable schematic/PCB data: `attempt-03/dist/artifacts/attempt-03/index/circuit.json`. Schematic SVG/PNG and PCB SVG/PNG are alongside that JSON. Run-root `package.json` and `bun.lock` preserve dependencies; CLI version 0.0.2462, Bun 1.3.14. Rebuild from the run root using `tsci build artifacts/attempt-03/index.circuit.tsx --svgs --pcb-png --disable-parts-engine` (writes run-root dist).

Shell-authored tscircuit TSX, documented standard footprinter packages, local autorouting. No GUI, KiCad design creation, publishing, or reference artifacts were used. J1/J2/J3 are 1x4 2.54 mm through-hole pinheaders, with pins 1–4 V3V3 (= +3V3), GND, SDA, SCL. R1/R2 are generic 0805 4.7 kohm resistors. C1/C2/C3 are generic 0805 100 nF capacitors, each 4 mm from its header centre. JP1/JP2 use two 1.5 mm square copper pads with a 0.3 mm open solderable gap; they are not bridged. No supplier-specific part numbers were selected.

All three attempts are retained. Attempt 01 failed to instantiate headers because internal pin label +3V3 was invalid. Attempt 02 repaired the identifier and made jumper gaps 0.3 mm; schematic placement requested narrower connector symbols. Attempt 03 applied that final repair. Netlist, schematic placement, PCB placement, build and shorts commands completed; final placement and shorts checks report no issues. The compiled circuit has 20 PCB traces, minimum emitted width 0.25 mm, and no error records. This is generation completion, not independent proof of all requirements.

Unresolved: the board JSON retains default 0.1 mm trace-to-pad and pad-to-pad clearance fields despite the attempted routingTolerances source settings. The autorouter was given traceClearance=0.25 mm, but full physical copper clearance is unmeasured and 0.25 mm compliance is not established. Two permitted repairs were exhausted. Generic pin-attribute warnings on jumpers, a missing schematic-sheet styling warning, and unnamed trace warnings remain. The schematic section heading is close to the first header label. No independent electrical/routing connectivity or manufacturing validation beyond the recorded CLI checks was performed. Raw diagnostics and measured geometry are under run-root logs; see result.json for the validation inventory.
