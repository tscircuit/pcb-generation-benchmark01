# Local and transient files
.DS_Store
.env
.env.*
!.env.example
.venv/
__pycache__/
*.py[cod]
node_modules/
work/
*.log
# KiCad local state and locks (native design files stay tracked)
*.kicad_prl
*.lck
~*.lck

# Benchmark diagnostic logs are retained as evidence.
!data/runs/**/*.log
!data/evaluations/**/*.log

# Ignore dependency symlinks as well as directories.
node_modules

# Generated application output (rebuild locally; do not commit).
/dist/
/website/dist/

# Vercel local project metadata
.vercel/
.vercel
.env*
